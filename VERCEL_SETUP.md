# Инструкция по настройке переменных окружения в Vercel

## 🔴 Проблема: DATABASE_URL is required

Если вы видите ошибку `DATABASE_URL is required` в логах Vercel, это означает, что переменные окружения не установлены.

## ✅ Решение: Добавление переменных окружения

### Шаг 1: Откройте настройки проекта в Vercel

1. Перейдите на https://vercel.com
2. Войдите в свой аккаунт
3. Выберите ваш проект (`aeronest-admin` или другое название)
4. Перейдите в **Settings** (Настройки)

### Шаг 2: Добавьте переменные окружения

1. В левом меню выберите **Environment Variables**
2. Нажмите **Add New** или **Add**

3. Добавьте первую переменную:
   - **Name:** `DATABASE_URL`
   - **Value:** Скопируйте значение из вашего `.env.local` файла
     ```
     postgresql://user:password@host:port/database?sslmode=require
     ```
   - **Environment:** Выберите все три:
     - ✅ Production
     - ✅ Preview  
     - ✅ Development

4. Нажмите **Save**

5. Добавьте вторую переменную:
   - **Name:** `AUTH_SECRET`
   - **Value:** Скопируйте значение из вашего `.env.local` файла
     Или сгенерируйте новый:
     ```bash
     openssl rand -base64 32
     ```
   - **Environment:** Выберите все три:
     - ✅ Production
     - ✅ Preview
     - ✅ Development

6. Нажмите **Save**

### Шаг 3: Перезапустите деплой

После добавления переменных:

1. Перейдите в раздел **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (⋯) справа
4. Выберите **Redeploy**
5. Подтвердите перезапуск

Или просто сделайте новый push в репозиторий - Vercel автоматически перезапустит деплой.

## 📋 Чеклист переменных окружения

Убедитесь, что добавлены:

- [ ] `DATABASE_URL` - URL вашей базы данных PostgreSQL
- [ ] `AUTH_SECRET` - Секретный ключ для NextAuth

## 🔍 Как проверить, что переменные установлены

1. В Vercel Dashboard откройте **Settings** → **Environment Variables**
2. Вы должны увидеть обе переменные в списке
3. Убедитесь, что они добавлены для нужных окружений

## ⚠️ Важные замечания

1. **Не коммитьте `.env.local` в Git**
   - Файл `.env.local` уже в `.gitignore`
   - Никогда не добавляйте секреты в репозиторий

2. **Используйте разные значения для разных окружений** (опционально)
   - Production: основная база данных
   - Preview: тестовая база данных
   - Development: локальная база данных

3. **После изменения переменных перезапустите деплой**
   - Vercel не применяет изменения автоматически
   - Нужно сделать Redeploy

## 🐛 Решение проблем

### Переменные добавлены, но ошибка остается

1. Проверьте, что переменные добавлены для правильного окружения (Production)
2. Убедитесь, что значения скопированы правильно (без лишних пробелов)
3. Сделайте Redeploy после добавления переменных

### Не знаю, где взять DATABASE_URL

Если вы используете Neon:
1. Откройте https://console.neon.tech
2. Выберите ваш проект
3. Скопируйте Connection String
4. Используйте его как `DATABASE_URL`

Если вы используете другого провайдера:
- Supabase: Settings → Database → Connection string
- Railway: Database → Connect → Connection URL
- Render: Database → Internal Database URL

### Ошибка "Invalid connection string"

Проверьте формат:
```
postgresql://username:password@host:port/database?sslmode=require
```

Убедитесь, что:
- Нет лишних пробелов в начале/конце
- Все специальные символы в пароле закодированы (URL encoding)
- SSL режим указан правильно

## 📞 Дополнительная помощь

Если проблема не решается:
1. Проверьте логи в Vercel Dashboard → Deployments → [ваш деплой] → Functions
2. Убедитесь, что база данных доступна из интернета
3. Проверьте firewall настройки базы данных

