# Руководство по работе с базой данных

Этот документ содержит инструкции по применению изменений схемы базы данных и работе с миграциями.

## 📋 Основные команды

### 1. Генерация миграций

После изменения схемы в `src/db/schema.ts` нужно сгенерировать миграцию:

```bash
npm run db:generate
```

**Что делает:**
- Анализирует изменения в схеме
- Создает SQL файл миграции в папке `drizzle/`
- Файл имеет формат: `000X_название_миграции.sql`

**Пример:**
```bash
npm run db:generate
# Создаст файл: drizzle/0005_mighty_absorbing_man.sql
```

### 2. Применение миграций (Push)

**Вариант A: Push (рекомендуется для разработки)**

Применяет изменения напрямую к базе данных без создания файлов миграций:

```bash
npm run db:push
```

**Что делает:**
- Сравнивает текущую схему с базой данных
- Автоматически применяет изменения
- **Внимание:** Может быть небезопасно в продакшене

**Когда использовать:**
- ✅ Локальная разработка
- ✅ Тестирование изменений
- ✅ Быстрые итерации

**Когда НЕ использовать:**
- ❌ Продакшен (используйте миграции)
- ❌ Когда нужна история изменений

### 3. Применение миграций (Migrate)

**Вариант B: Migrate (рекомендуется для продакшена)**

Применяет сохраненные миграции из папки `drizzle/`:

```bash
npm run db:migrate
```

**Что делает:**
- Применяет все непримененные миграции из папки `drizzle/`
- Отслеживает примененные миграции в базе данных
- Безопаснее для продакшена

**Когда использовать:**
- ✅ Продакшен
- ✅ Когда нужна история изменений
- ✅ Командная разработка

## 🔄 Типичный рабочий процесс

### Для локальной разработки:

```bash
# 1. Измените схему в src/db/schema.ts
# Например, добавили новое поле в таблицу users

# 2. Примените изменения
npm run db:push

# 3. Проверьте, что все работает
npm run dev
```

### Для продакшена:

```bash
# 1. Измените схему в src/db/schema.ts

# 2. Сгенерируйте миграцию
npm run db:generate

# 3. Проверьте созданный файл миграции
# Откройте drizzle/000X_название.sql и убедитесь, что SQL корректен

# 4. Примените миграцию локально для проверки
npm run db:migrate

# 5. Закоммитьте миграцию в Git
git add drizzle/
git commit -m "feat: add new field to users table"
git push

# 6. На Vercel миграция применится автоматически при деплое
# Или примените вручную через CLI/консоль базы данных
```

## 📝 Примеры изменений схемы

### Добавление новой таблицы

```typescript
// src/db/schema.ts
export const newTable = pgTable("new_table", {
  id: text("id").primaryKey().$defaultFn(() => createId()),
  name: text("name").notNull(),
  createdAt: timestamp("created_at").notNull().defaultNow(),
});
```

Затем:
```bash
npm run db:push
```

### Добавление поля в существующую таблицу

```typescript
// src/db/schema.ts
export const users = pgTable("users", {
  // ... существующие поля
  newField: text("new_field"), // Новое поле
});
```

Затем:
```bash
npm run db:push
```

### Изменение типа поля

```typescript
// Было:
balance: decimal("balance", { precision: 10, scale: 2 })

// Стало:
balance: decimal("balance", { precision: 12, scale: 2 })
```

Затем:
```bash
npm run db:generate  # Создаст миграцию
npm run db:migrate  # Применит миграцию
```

## 🛠️ Дополнительные команды

### Просмотр схемы в браузере (Drizzle Studio)

```bash
npm run db:studio
```

**Что делает:**
- Открывает веб-интерфейс для просмотра данных
- Позволяет редактировать данные напрямую
- Полезно для отладки и тестирования

**Доступ:** Обычно открывается на http://localhost:4983

### Удаление всех таблиц (ОСТОРОЖНО!)

```bash
npm run db:drop
```

**⚠️ ВНИМАНИЕ:** Эта команда удалит ВСЕ таблицы из базы данных!

**Используйте только:**
- Для полного сброса локальной БД
- При разработке
- **НИКОГДА** в продакшене!

## 🔍 Проверка состояния базы данных

### Проверка подключения

```bash
npm run db:test
```

Проверяет, что подключение к базе данных работает.

### Просмотр миграций

Миграции хранятся в папке `drizzle/`:

```bash
ls drizzle/
# Покажет все файлы миграций:
# 0001_initial.sql
# 0002_add_users.sql
# 0003_add_orders.sql
# ...
```

## ⚠️ Важные замечания

### 1. Всегда проверяйте миграции перед применением

```bash
# Сгенерируйте миграцию
npm run db:generate

# Откройте файл и проверьте SQL
cat drizzle/000X_название.sql
```

### 2. Делайте бэкап перед применением в продакшене

```bash
# Создайте бэкап базы данных перед миграцией
pg_dump $DATABASE_URL > backup.sql
```

### 3. Не коммитьте `.env.local`

Файл `.env.local` уже в `.gitignore`, но убедитесь, что:
- ✅ `DATABASE_URL` не в Git
- ✅ Пароли не в Git
- ✅ Секретные ключи не в Git

### 4. Разные базы для разных окружений

Рекомендуется использовать:
- **Локально:** Локальная PostgreSQL или Neon dev база
- **Preview:** Тестовая база данных
- **Production:** Основная продакшен база

## 🚀 Применение миграций на Vercel

### Автоматическое применение

Vercel автоматически применяет миграции при деплое, если:

1. Миграции закоммичены в Git
2. В `package.json` есть скрипт `db:migrate`
3. Переменная `DATABASE_URL` установлена

### Ручное применение

Если нужно применить миграции вручную:

1. Подключитесь к базе данных через CLI
2. Или используйте веб-интерфейс вашего провайдера БД (Neon, Supabase и т.д.)
3. Выполните SQL из файла миграции

### Через Neon Console

1. Откройте https://console.neon.tech
2. Выберите ваш проект
3. Перейдите в SQL Editor
4. Скопируйте содержимое файла миграции
5. Выполните SQL

## 📊 Структура миграции

Пример файла миграции:

```sql
-- drizzle/0005_mighty_absorbing_man.sql

CREATE TABLE "referral_uses" (
	"id" text PRIMARY KEY NOT NULL,
	"referral_id" text NOT NULL,
	"user_id" text NOT NULL,
	"created_at" timestamp DEFAULT now() NOT NULL
);

ALTER TABLE "referrals" ADD COLUMN "max_uses" text;

ALTER TABLE "referral_uses" ADD CONSTRAINT "referral_uses_referral_id_referrals_id_fk" 
FOREIGN KEY ("referral_id") REFERENCES "public"."referrals"("id") 
ON DELETE cascade ON UPDATE no action;
```

## 🔧 Решение проблем

### Ошибка "DATABASE_URL is required"

**Решение:**
1. Убедитесь, что `.env.local` существует
2. Проверьте, что `DATABASE_URL` установлен
3. Перезапустите терминал/сервер

### Ошибка "relation already exists"

**Причина:** Таблица уже существует в базе данных

**Решение:**
```bash
# Проверьте текущее состояние БД
npm run db:studio

# Или удалите таблицу вручную (ОСТОРОЖНО!)
# Или используйте IF NOT EXISTS в миграции
```

### Ошибка "migration already applied"

**Причина:** Миграция уже была применена

**Решение:**
- Это нормально, просто пропустите эту миграцию
- Или удалите запись о миграции из таблицы `__drizzle_migrations`

### Конфликт миграций

**Причина:** Несколько разработчиков создали миграции одновременно

**Решение:**
1. Синхронизируйтесь с командой
2. Переименуйте файлы миграций в правильном порядке
3. Примените по очереди

## 📚 Полезные ссылки

- [Drizzle ORM Documentation](https://orm.drizzle.team/docs/overview)
- [Drizzle Kit CLI](https://orm.drizzle.team/kit-docs/overview)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

## ✅ Чеклист перед применением миграций

- [ ] Изменения в `src/db/schema.ts` сохранены
- [ ] Сгенерирована миграция (`npm run db:generate`)
- [ ] Проверен SQL файл миграции
- [ ] Создан бэкап (для продакшена)
- [ ] Протестировано локально
- [ ] Закоммичено в Git
- [ ] Готово к применению в продакшене

