# Инструкция по деплою проекта

## 📋 Подготовка к деплою

### Шаг 1: Проверка локальной работы

Перед деплоем убедитесь, что проект работает локально:

```bash
# Установите зависимости
npm install

# Проверьте, что билд проходит успешно
npm run build

# Запустите локально и проверьте регистрацию
npm run dev
```

### Шаг 2: Подготовка переменных окружения

Убедитесь, что у вас есть файл `.env.local` с переменными:

```env
DATABASE_URL=postgresql://ваш-url-базы-данных
AUTH_SECRET=ваш-секретный-ключ
```

**Важно:** Скопируйте эти значения - они понадобятся при настройке Vercel.

## 🚀 Деплой на Vercel (Рекомендуется)

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Название репозитория: `aeronest-dron` (или другое)
3. Выберите **Private** или **Public**
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите **Create repository**

### Шаг 2: Отправка кода на GitHub

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте remote репозиторий
git remote add origin https://github.com/ваш-username/aeronest-dron.git

# Проверьте текущий remote
git remote -v

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "feat: готовый проект для деплоя"

# Отправьте на GitHub
git push -u origin main
```

### Шаг 3: Деплой на Vercel

1. **Перейдите на https://vercel.com** и войдите через GitHub
2. Нажмите **Add New Project**
3. Импортируйте репозиторий с GitHub
4. Выберите ваш репозиторий

### Шаг 4: Настройка переменных окружения в Vercel

**КРИТИЧЕСКИ ВАЖНО:** Добавьте переменные окружения ДО деплоя:

1. В разделе **Environment Variables** добавьте:

   ```
   DATABASE_URL = postgresql://ваш-url-базы-данных
   AUTH_SECRET = ваш-секретный-ключ
   ```

2. Убедитесь, что переменные добавлены для всех окружений:

   - ✅ Production
   - ✅ Preview
   - ✅ Development

3. **Как получить AUTH_SECRET:**
   ```bash
   # Сгенерируйте новый секретный ключ
   openssl rand -base64 32
   ```
   Или используйте онлайн генератор: https://generate-secret.vercel.app/32

### Шаг 5: Настройки билда

Vercel автоматически определит Next.js, но проверьте:

- **Framework Preset:** Next.js ✅
- **Build Command:** `npm run build` ✅
- **Output Directory:** `.next` ✅
- **Install Command:** `npm install` ✅

### Шаг 6: Деплой

1. Нажмите **Deploy**
2. Дождитесь завершения билда (обычно 2-5 минут)
3. После успешного деплоя вы получите URL вида: `https://ваш-проект.vercel.app`

## ✅ Проверка после деплоя

### 1. Проверка основных страниц

- ✅ Главная страница: `https://ваш-проект.vercel.app`
- ✅ Каталог: `https://ваш-проект.vercel.app/каталог` (или через навигацию)

### 2. Проверка регистрации

1. Откройте главную страницу
2. Нажмите кнопку входа/регистрации
3. Перейдите на вкладку "Регистрация"
4. Заполните форму:
   - Email: `test@example.com`
   - Пароль: минимум 6 символов
   - Имя: любое
   - **Реферальный код:** обязателен (нужно создать в админ-панели)
5. Нажмите "Зарегистрироваться"

**Если реферальный код обязателен:**

- Сначала создайте админа (см. ниже)
- Войдите в админ-панель
- Создайте реферальный код
- Используйте его при регистрации

### 3. Создание первого администратора

После деплоя нужно создать первого админа. Есть два способа:

#### Способ 1: Через регистрацию (если реферальный код не обязателен)

1. Зарегистрируйтесь с email: `admin@example.com`
2. Войдите в систему
3. Откройте базу данных и обновите роль пользователя (если нужно)

#### Способ 2: Через базу данных напрямую

1. Подключитесь к вашей базе данных (Neon, Supabase и т.д.)
2. Найдите таблицу `users`
3. Создайте пользователя с email `admin@example.com`
4. Хешируйте пароль с помощью bcrypt

Или используйте скрипт (если есть доступ к серверу):

```bash
npm run db:create-admin
```

### 4. Проверка админ-панели

1. Войдите как администратор (`admin@example.com`)
2. Откройте: `https://ваш-проект.vercel.app/admin`
3. Проверьте доступные разделы:
   - ✅ Дашборд
   - ✅ Все заказы
   - ✅ Пользователи
   - ✅ Рефералы

### 5. Создание реферального кода

1. Войдите в админ-панель
2. Перейдите в раздел "Рефералы"
3. Нажмите "Создать код"
4. Заполните:
   - Реферальный код: `WELCOME2024` (например)
   - ID пользователя: ID админа или другого пользователя
   - Максимальное количество использований: (необязательно)
5. Сохраните

Теперь этот код можно использовать при регистрации новых пользователей.

## 🔧 Решение проблем

### Ошибка "DATABASE_URL not found"

**Решение:**

1. Убедитесь, что переменная `DATABASE_URL` добавлена в Vercel
2. Проверьте, что значение правильное (скопировано из `.env.local`)
3. После добавления переменных перезапустите деплой

### Ошибка "AUTH_SECRET is required"

**Решение:**

1. Добавьте `AUTH_SECRET` в Environment Variables в Vercel
2. Сгенерируйте новый ключ: `openssl rand -base64 32`
3. Перезапустите деплой

### Ошибка билда

**Решение:**

1. Проверьте логи билда в Vercel Dashboard
2. Убедитесь, что все зависимости установлены
3. Проверьте, что нет ошибок TypeScript

### Регистрация не работает

**Возможные причины:**

1. Реферальный код обязателен, но не создан
2. База данных не подключена
3. Проверьте логи в Vercel Functions

**Решение:**

1. Создайте реферальный код в админ-панели
2. Проверьте подключение к базе данных
3. Посмотрите логи в разделе Functions в Vercel

### Админ-панель недоступна

**Решение:**

1. Убедитесь, что вы вошли как `admin@example.com`
2. Проверьте, что пользователь существует в базе данных
3. Проверьте логи аутентификации

## 📝 Дополнительные настройки

### Настройка домена (опционально)

1. В Vercel Dashboard перейдите в Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Vercel

### Настройка автоматического деплоя

По умолчанию Vercel автоматически деплоит при каждом push в main ветку.

Для настройки других веток:

1. Settings → Git
2. Настройте Production Branch и другие опции

## 🎉 Готово!

После успешного деплоя ваш проект будет доступен по адресу:
`https://ваш-проект.vercel.app`

Все функции должны работать:

- ✅ Регистрация пользователей
- ✅ Вход в систему
- ✅ Каталог магазинов
- ✅ Создание заказов
- ✅ Админ-панель
- ✅ Управление пользователями
- ✅ Управление заказами
- ✅ Реферальная система

## 4. Альтернатива: Деплой на другие платформы

### Netlify

1. Подключите репозиторий на https://netlify.com
2. Добавьте переменные окружения в настройках проекта
3. Build command: `npm run build`
4. Publish directory: `.next`

### Railway

1. Подключите репозиторий на https://railway.app
2. Добавьте переменные окружения в настройках проекта
3. Railway автоматически определит Next.js

## 5. Проверка после деплоя

После успешного деплоя проверьте:

1. ✅ Главная страница открывается
2. ✅ Регистрация работает
3. ✅ Вход работает
4. ✅ Админ-панель доступна для admin@example.com
5. ✅ Каталог загружается из БД

## Решение проблем

### Ошибка "DATABASE_URL not found"

Убедитесь, что переменная `DATABASE_URL` добавлена в настройках платформы деплоя.

### Ошибка "Repository not found"

1. Проверьте, что репозиторий существует на GitHub
2. Проверьте права доступа к репозиторию
3. Убедитесь, что используете правильный URL

### Ошибка билда

Проверьте логи билда на платформе деплоя для деталей ошибки.
